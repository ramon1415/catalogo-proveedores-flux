// Compatibility entrypoint retained for the dispatcher and its contract tests.
// The implementation lives in system_pdf_renderer.ts and mirrors
// approval_batches.js::exportPdf with the same logo, libraries and layout.
export * from "./system_pdf_renderer.ts";
