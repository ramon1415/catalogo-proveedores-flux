name: CONTPAQ FB-2 contract

on:
  pull_request:
    paths:
      - "contpaq_cfdi_validation.js"
      - "scripts/qa/contpaq-fb2-cfdi-validation.test.mjs"
      - "docs/architecture/contpaq-fb2-fb3-dev-contract.md"
      - ".github/workflows/contpaq-fb2-contract.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: JavaScript syntax
        run: node --check contpaq_cfdi_validation.js
      - name: FB-2 validation contract
        run: node --test scripts/qa/contpaq-fb2-cfdi-validation.test.mjs
